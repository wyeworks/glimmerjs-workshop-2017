<div class="group-title"><span>Group {{groupLetter}}</span></div>

<ul class="teams">
  {{#each @teams key="@index" as |team|}}
    {{#if team}}
      <li>{{team.name}}</li>
    {{else}}
      <li>&nbsp;</li>
    {{/if}}
  {{/each}}
</ul>
